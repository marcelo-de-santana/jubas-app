import {Screen, Text} from '@components';
import {BusinessManagementScreenProps} from '@routes';

export function CategoryCreateScreen({
  navigation,
}: BusinessManagementScreenProps) {
  return (
    <Screen>
      <Text>Tela para criar categorias</Text>
    </Screen>
  );
}
