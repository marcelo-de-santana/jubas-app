import {EmployeeServicesListScreenProps} from '@routes';

export function EmployeeServicesListScreen({
  route,
}: EmployeeServicesListScreenProps) {
  const {employee} = route.params;
  return <></>;
}
